import { Express, Handler } from 'express';
import { controllers } from '../controllers';
import { middleware } from '../middleware';

export function periodRoutes(basePath: string, app: Express) {
	// app.get( `${basePath}/period`, controllers.period.getPeriods);
	// app.post( )
	// app.put( )
	// app.delete( )
}
