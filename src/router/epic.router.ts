import { Express, Handler } from 'express';
import { controllers } from '../controllers';
import { middleware } from '../middleware';

export function epicRoutes(basePath: string, app: Express) {
	// app.get( `${basePath}/epic`, controllers.period.getPeriods);
	// app.post( )
	// app.put( )
	// app.delete( )
}
